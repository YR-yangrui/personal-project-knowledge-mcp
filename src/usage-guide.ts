export const usageGuideMarkdown = `# Personal Project Knowledge MCP 使用指南

这是一个个人知识库 / 记忆管理 MCP。它的职责类似长期可用的个人 skill：在会话开始时给 AI 注入稳定规则，并提供工具管理记忆、Markdown 文档、项目决策和需求变动记录。

## 优先使用规则

- 遇到“记住、偏好、习惯、项目规则、踩坑、约定”时，优先使用本 MCP 写入或更新记忆。
- 遇到“文档、设计、决策记录、需求变动、分析沉淀、长内容”时，优先使用本 MCP 写入 Markdown 文档，并创建或更新长记忆索引。
- 需要查历史经验、项目规则、需求变更、已有文档时，优先使用本 MCP 的记忆 / 文档搜索；代码定位仍遵循当前项目自己的代码检索规则。
- 不要把长正文塞进短记忆；短记忆只放会话开始必须全文载入的内容。
- 不确定一条信息是否应该自动载入时，默认写成文档或长记忆索引，而不是短记忆。

## 内容分层

- 短记忆 short_memory：每次自动全文载入，适合全局偏好、强约束、项目规则、简短 gotcha。
- 长记忆 long_index：每次只载入标题、摘要和关联文档路径，适合项目决策、需求变动、较长经验。
- 文档 document：Markdown 正文，按需搜索和读取，适合完整方案、分析报告、会议记录、导入资料。

## 常用操作

- 会话开始：调用 build_context，或读取 hook 自动注入的上下文。
- 查看自动载入内容：list_loaded_memory。
- 写入记忆：write_memory，短记忆必须简短；长内容先写文档。
- 搜索记忆：search_memory，必要时 get_memory。
- 写入文档：write_doc，然后 create_or_update_doc_index。
- 搜索 / 读取文档：search_docs 返回索引，read_doc 读取正文。
- 内容过长：demote_memory_to_doc，把短记忆降级为文档 + 长索引。

## 写入判断

- 用户明确说“记住”：优先写短记忆；如果内容过长，写文档 + 长索引。
- 用户给出项目规范或长期约束：写 project_rule，通常是短记忆。
- 用户确定一个方案或取舍：写 decision，通常是文档 + 长索引。
- 用户修改需求或推翻旧方案：写 requirement_change，并废弃或更新旧记忆。
- 会话中产出的完整分析、设计、排查过程：写 document，并生成 long_index。

## 重要边界

- 记忆的定义是“会自动载入的内容”；不会自动载入的长正文应归类为文档。
- 短记忆可以直接作为已知事实使用。
- 长记忆索引只说明存在相关资料，不代表正文已读；需要细节时必须 read_doc。
- 这个 MCP 是个人使用系统，不处理团队权限、协同审批或多人知识治理。
`;
